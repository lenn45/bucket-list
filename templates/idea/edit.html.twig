{% extends 'layout.html.twig' %}

{% block title %}Modifier une idée{% endblock %}

{% block main %}
<h2>Modifier une idée</h2>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Mettre à jour</button>
{{ form_end(form) }}

<a href="{{ path('list') }}" class="btn btn-secondary">Annuler</a>
{% endblock %}
